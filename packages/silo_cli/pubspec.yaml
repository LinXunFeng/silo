name: silo_cli
description: Command-line front end for Silo — add models to the store and link them into local tools.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

executables:
  silo: silo

dependencies:
  args: ^2.6.0
  silo_core: ^0.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
